apiVersion: v1
kind: Service
metadata:
  name: {{ include "CD-product-service.fullname" . }}
  labels:
    {{- include "CD-product-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "CD-product-service.selectorLabels" . | nindent 4 }}
