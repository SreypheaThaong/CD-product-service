apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "CD-product-service.fullname" . }}-test-connection"
  labels:
    {{- include "CD-product-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "CD-product-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
